# КОНТЕКСТ ПРОЕКТА: SaldaCargo (17.05.2026 — сессия 12)

> Этот файл — точка входа для AI-ассистента. Прочитав его, ты знаешь где мы находимся и что делать дальше.
> База знаний: `kb/wiki/` — читай соответствующие статьи перед задачами.

---

## 1. Что за проект

SaldaCargo — ERP-система для транспортной компании (Верхняя Салда).  
Цель: в будущем продать как коробочное решение для других транспортных компаний.

**Два приложения:**

- `apps/miniapp` — Next.js 15, мобильный интерфейс для водителей/механиков/админа (мессенджер MAX)
- `apps/web` — Next.js 15, десктопный дашборд для владельца и админа

**Стек:** Next.js 15, React 19, TypeScript, Supabase (PostgreSQL), TanStack Query, Tailwind CSS 4  
**Auth:** кастомная cookie-схема (`salda_user_id`), НЕ Supabase Auth  
**Деплой:** Vercel (miniapp и webapp — оба задеплоены, автодеплой из `main`) — **всегда пушить после коммита**

---

## 2. Что уже реализовано

### MiniApp (apps/miniapp) — ГОТОВО ✅

- **Авторизация:** выбор роли → выбор имени → PIN-код для администраторов
- **Водитель:** создание рейса, заказы, расходы, завершение с одометром, заявка на ремонт (через repair_requests + fault_catalog)
- **Механик:** наряды (мои + свободные + claim), таймеры, запчасти, доп. работы (согласование), ЗП за месяц
- **Админ (мобильный):**
  - Дашборд KPI
  - Рейсы: На ревью / Активные / История (навигация по датам)
  - Финансы: журнал транзакций (кнопки +Доход/-Расход — на главном экране!)
  - **+Доход / −Расход:** на главном экране → подменю:
    - Доход: Добавить доход / Инкассация / Дебиторка
    - Расход: Добавить расход / Долг поставщику / Кредиты / ЗП сотрудников
  - **Долг поставщику:** `/api/admin/payable-debt` POST — фиксирует взятые в кредит запчасти
  - **Дебиторка:** список должников → заказы → погасить
  - **Журнал транзакций:** навигация по дням (← →) и месяцам, итоги за день
  - **Долги поставщиков** (`/api/admin/payables`): Дерябин ГСМ / Новиков / Ромашин
  - **Гараж** `/admin/garage` — 5 вкладок:
    - Заявки водителей (из repair_requests)
    - Доп. работы на согласовании
    - Наряды на утверждении (approve → начисляет ЗП механику)
    - Активные наряды
    - Алерты ТО (из maintenance_items)

### WebApp (apps/web)

| Раздел            | Статус      | Что работает                                                                                                                        |
| ----------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `/` (главная)     | ✅          | KPI за месяц + плитки кошельков с корректировкой остатка                                                                            |
| `/review`         | ✅          | Одобрение/возврат рейсов + история + итоги за день/месяц                                                                            |
| `/retro`          | ✅          | Ручной ввод рейса задним числом                                                                                                     |
| `/finance`        | ✅          | P&L за 6 месяцев + журнал по дням/месяцам + подотчёт водителей                                                                      |
| `/receivables`    | ✅          | Дебиторка — список должников, кнопка "Оплачено", исторические долги                                                                 |
| `/counterparties` | ✅          | Постоянные / Разовые, карточка клиента с аналитикой                                                                                 |
| `/fleet`          | ✅          | Автопарк — коллапсируемые плитки, юнит-экономика                                                                                    |
| `/staff`          | ✅          | Персонал — выплаты ЗП, CRUD                                                                                                         |
| `/loans`          | ✅          | Кредиты и займы                                                                                                                     |
| `/payables`       | ✅          | Долги поставщикам — Дерябин ГСМ / Новиков / Ромашин                                                                                 |
| `/garage`         | ⚠️ ЧАСТИЧНО | Только 3 вкладки: На ревью / Активные / История (наряды). По референсу `garage-design.html` нужно значительно больше — см. раздел 3 |

---

## 3. Гараж WebApp — что реализовано vs что нужно по референсу

Референс: `garage-design.html` в корне репо.

### Реализовано сейчас (`/garage`)

- Три вкладки: **На ревью** (lifecycle=draft) / **Активные** (lifecycle=approved, status=created|in_progress) / **История** (по дате)
- Карточка наряда: раскрывается, показывает работы, запчасти, итог по наряду
- Кнопки: Одобрить / Вернуть / Отменить (на вкладке «На ревью»)
- Создание нового наряда (своя машина / клиентская)
- Боковая панель детали наряда: редактировать статус, приоритет, механика, заметку

### Не реализовано (есть в референсе, нужно строить)

- **Дашборд** — KPI плитки: активные наряды / на ревью / заявки / алерты ТО + сводка за текущий день
- **Заявки водителей** — отдельная секция из `repair_requests` (сейчас только в MiniApp)
- **Регламенты ТО** — управление `maintenance_items` по автомобилю
- **Справочник видов работ** — CRUD `work_catalog` с нормачасами
- **Каталог неисправностей** — CRUD `fault_catalog`
- **Отчёт по автомобилям** — история нарядов по машине
- **Отчёт по механикам** — ЗП и нормачасы за период

---

## 4. Что сделано в сессии 12 (17.05.2026) — Модуль Гараж

### Миграции БД — ✅ ПРИМЕНЕНЫ В SUPABASE

**`20260517000001_garage_module.sql`:**

- Новые таблицы: `sto_settings`, `repair_requests`, `fault_catalog`, `maintenance_items`
- Расширения: `users.mechanic_salary_pct`, `service_orders.second_mechanic_id`, `service_order_works.extra_work_*`, `work_catalog.norm_minutes_valdai`
- Данные: 12 неисправностей в `fault_catalog`, 16 работ в `work_catalog`

**`20260517000002_sto_dual_rates.sql`:**

- Добавлено `sto_settings.hourly_rate_own = 1600.00` (ставка для своего автопарка)

### Два тарифа нормачаса

При утверждении наряда (`PATCH /api/admin/service-orders/:id action=approve`) тариф выбирается по `service_orders.machine_type`:

- `'client'` → `sto_settings.hourly_rate` (2 000 ₽/ч)
- `'own'` → `sto_settings.hourly_rate_own` (1 600 ₽/ч)

ЗП механика = `normHours * hourlyRate * mechanic_salary_pct / 100`  
При двух механиках — нормочасы делятся 50/50.

### API MiniApp (новые эндпоинты)

- `GET/POST /api/driver/repair-requests` — заявки водителей
- `GET /api/driver/fault-catalog` — каталог неисправностей
- `POST /api/mechanic/orders/{id}/claim` — взять свободный наряд
- `POST /api/mechanic/orders/{id}/extra-work` — запрос доп. работы
- `GET /api/mechanic/salary` — ЗП механика за месяц
- `GET /api/admin/garage` — дашборд гаража
- `GET/PATCH /api/admin/repair-requests/{id}` — управление заявками
- `PATCH /api/admin/service-orders/{id}` — approve/return/cancel/extra-work
- `GET /api/admin/mechanics` — список механиков с mechanic_salary_pct

### MiniApp страницы

- Механик: вкладки «Мои»/«Свободные» + claim, доп. работа flow, страница ЗП
- Администратор: `/admin/garage` с 5 вкладками + все модальные окна
- Водитель: форма заявки через repair_requests + fault catalog + история заявок
- Навбар администратора: добавлена вкладка «Гараж 🛠»

**Типы Supabase (`pnpm gen:types`) — НЕ обновлены.** Требует Docker Desktop.

---

## 5. Что сделано в сессии 11 (16.05.2026)

### /counterparties — рефакторинг и аналитика

- Два таба: **Постоянные** (список + карточка) и **Разовые** (агрегированная аналитика)
- Карточка клиента: 4 KPI-плитки, breakdown ЧП, барчарт по месяцам, история рейсов
- Формула ЧП: `Выручка − ЗП_водителя − ЗП_грузчика − ГСМ_пропорциональный − Выручка × overhead_pct`
- API: `GET /api/counterparties/[id]/trips`

### Унификация фильтра выручки (везде одинаково)

```
trip_orders.lifecycle_status = 'approved'
trip_orders.settlement_status = 'completed'
trips.lifecycle_status = 'approved'
```

### Другие исправления

- **Подотчёт водителя** — только `payment_method = 'cash'` (не card_driver)
- **Долг поставщику** — кнопка в MiniApp, новый API `/api/admin/payable-debt`
- **Кнопки Доход/Расход** — только на главном экране MiniApp, убраны из Finance
- **Баг set-balance** — добавлены CAT_OTHER_INCOME / CAT_OTHER_EXPENSE
- **Баг /api/admin/transactions** — добавлена валидация category_id и amount

---

## 6. КРИТИЧЕСКАЯ ПРОБЛЕМА С ДАННЫМИ

### ЗП "закрытие подотчёта" без привязки к сотруднику

9 транзакций с `related_user_id = NULL` на ~173 750 ₽ + Шайдулин 4 100 ₽ = **~177 850 ₽ расхождение Finance vs Staff**.

Внесены вручную через "Добавить расход → ЗП водителя" вместо кнопки "ЗП сотрудников".

**Исправление:** UPDATE в Supabase SQL Editor:

1. `SELECT id, name FROM users WHERE is_active = true ORDER BY name;`
2. Найти транзакции по description, проставить related_user_id

### Расхождение выручки Finance vs Counterparties: 252 900 ₽

26 заказов без `counterparty_id`. Finance считает все, Counterparties — только с клиентом.

---

## 7. Архитектура учёта ЗП

| Источник                               | Что даёт                 | Где используется            |
| -------------------------------------- | ------------------------ | --------------------------- |
| `trip_orders.driver_pay / loader_pay`  | Начисленная ЗП из рейсов | P&L, "заработано" на /staff |
| `transactions` (PAYROLL\_\* + ADVANCE) | Факт выплаты денег       | "выплачено" на /staff       |

`debt = MAX(earned - paid, 0)` где `paid` = PAYROLL + ADVANCE.

**Транзакции ЗП вносить ТОЛЬКО через кнопку "ЗП сотрудников"** — иначе `related_user_id = null`.

---

## 8. Категории транзакций (UUID)

### Расходы

| name                     | code             | id                                   |
| ------------------------ | ---------------- | ------------------------------------ |
| ЗП водителя              | PAYROLL_DRIVER   | d79213ee-3bc6-4433-b58a-ca7ea1040d00 |
| ЗП грузчика              | PAYROLL_LOADER   | 18792fa8-fda8-472d-8e04-e19d2c6c053c |
| ЗП механика              | PAYROLL_MECHANIC | 3d174f9f-34c2-4bc8-a3a9-d82f96f85bf6 |
| Аванс сотруднику         | ADVANCE_PAYMENT  | a0000000-0000-0000-0000-000000000001 |
| ГСМ                      | FUEL             | 62cebf3f-9982-4cc6-904b-48c6169cf5e4 |
| Запчасти                 | REPAIR_PARTS     | 9d18370d-3228-4f2a-8530-52b168cfa8d7 |
| Сторонний ремонт         | REPAIR_EXTERNAL  | 8c5f3080-d0a8-49ea-b759-3286f2084940 |
| Налоги                   | TAX              | f4a66f83-63c0-4ba0-8eaa-0d42ce19d3ca |
| Страховка                | INSURANCE        | 992ed3ae-66a7-4a16-a2da-cecd9b7086c5 |
| Офис и связь             | OFFICE_COMMS     | 73f565eb-f509-4538-9658-9cdff69bee37 |
| Прочий расход            | OTHER_EXPENSE    | df1022df-4ea6-46fc-b9aa-f3c9eb4e7f30 |
| Перевод между кошельками | WALLET_TRANSFER  | b9946a5e-4a33-4ed9-a272-5dee12d4ca93 |

### Доходы

| name              | code            | id                                   |
| ----------------- | --------------- | ------------------------------------ |
| Выручка с рейса   | TRIP_REVENUE    | 74008cf7-0527-4e9f-afd2-d232b8f8125a |
| Выручка с ремонта | SERVICE_REVENUE | 600e7f70-2797-474d-948b-432230036d67 |
| Продажа актива    | ASSET_SALE      | 84562648-767a-47b8-8821-a5c938da6e75 |
| Прочий доход      | OTHER_INCOME    | 68225ea2-d7de-4442-8ed8-ce2366b5d369 |

---

## 9. Архитектура расчётов

| Раздел                | Источник выручки                                                                     | Источник расходов                                             |
| --------------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------------------- |
| **P&L (/finance)**    | `trip_orders` (lifecycle=approved + settlement=completed + trips.lifecycle=approved) | `transactions` (expense, approved, completed)                 |
| **Контрагенты**       | то же (унифицировано с Finance)                                                      | —                                                             |
| **Автопарк (/fleet)** | `trip_orders.amount` (approved)                                                      | `trip_expenses` + `driver_pay/loader_pay` + `part_movements`  |
| **Балансы кошельков** | `transactions` (income, approved, completed, to_wallet_id)                           | `transactions` (expense, approved, completed, from_wallet_id) |
| **Дебиторка**         | `trip_orders` (settlement=pending) + `manual_receivables` (settled=false)            | —                                                             |

### Долги поставщиков

Долг = SUM(expense, pending, approved) − SUM(expense, completed, approved) по `counterparty_id`

```
20000000-0000-0000-0000-000000000001 — Дерябин ГСМ
20000000-0000-0000-0000-000000000002 — Новиков А.В. Запчасти
20000000-0000-0000-0000-000000000003 — Ромашин Запчасти
```

---

## 10. Следующие шаги (приоритет по порядку)

### ШАГ 1 — WebApp /garage: реализовать по референсу `garage-design.html`

Текущая страница `/garage` — только 3 вкладки нарядов. Нужно дорастить до референса:

1. **Дашборд** — KPI плитки: активных нарядов / на ревью / заявок водителей / алертов ТО
2. **Заявки водителей** — секция из `repair_requests` (approve → создать наряд / reject)
3. **Регламенты ТО** — список `maintenance_items` по авто, редактирование дат/пробегов
4. **Справочники**: `work_catalog` (CRUD нормачасов) + `fault_catalog` (CRUD)
5. **Отчёты**: история нарядов по машине + ЗП/нормачасы механиков за период

### ШАГ 2 — Исправить данные: related_user_id для 9 транзакций ЗП

SQL в Supabase: SELECT id, name FROM users; → UPDATE transactions SET related_user_id = ...

### ШАГ 3 — Обновить типы Supabase (когда Docker запущен)

```bash
pnpm gen:types
git add packages/shared-types/src/database.types.ts
git commit -m "chore: обновить типы Supabase — модуль Гараж"
```

### ШАГ 4 — Защита: убрать PAYROLL\_\* из формы "Добавить расход" в MiniApp

Категории ЗП не должны быть доступны через ручной ввод расхода.

### ШАГ 5 — Разобраться с 26 заказами без counterparty_id (252 900 ₽)

Проверить и привязать к контрагентам задним числом.

---

## 11. Ключевые технические факты

- `users.roles` — массив: `.contains('roles', ['driver'])`, НЕ `.eq('role', ...)`
- Все API-роуты: `createAdminClient()` + `(supabase.from('table') as any)`
- Деньги — только строкой: `"5000.00"`, компонент `<Money amount="5000.00" />`
- `crypto.randomUUID()` для idempotency_key (НЕ пакет `uuid`)
- `useSearchParams()` в Next.js 15 — обязательно в `<Suspense>`
- WebApp работает **без cookie** (auth отключена локально) → API не требуют cookie
- Скроллируемый flex-child: обязательно `min-h-0` + `overflow-y-auto`
- Bottom-sheet в MAX WebView: `position:fixed` на body + `paddingBottom: calc(env(safe-area-inset-bottom,0px) + 56px)`
- Двухшаговое удаление: boolean state + useRef таймер 4 сек авто-сброс
- Новые таблицы Supabase требуют `GRANT ALL ON TABLE ... TO postgres, anon, authenticated, service_role`

### ID кошельков

```
10000000-0000-0000-0000-000000000001 — Расчётный счёт
10000000-0000-0000-0000-000000000002 — Сейф (Наличные)
10000000-0000-0000-0000-000000000003 — Карта
```

### PIN-коды администраторов

- Нигамедьянов А.С. → `3030`
- Шахмаев А.О → `9111`

---

_Обновлён: 17.05.2026 (сессия 12). Исправлен статус /garage WebApp (частично, не соответствует референсу). Приоритет #1 — дорастить /garage до garage-design.html._
