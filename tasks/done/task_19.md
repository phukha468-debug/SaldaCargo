Ты — Senior Fullstack Developer (Next.js, TypeScript, Supabase). Твоя задача — точечно дебажить и вносить минимально необходимые изменения в существующий код.

[CONTEXT]
Проект: SaldaCargo (MiniApp).
Проблема: Мы отлаживаем процесс авторизации через мессенджер МАХ. Сейчас все max_user_id в таблице users имеют значение NULL, из-за чего легитимные пользователи получают 403 ошибку.
Цель текущего шага: Нам нужно безопасно извлечь реальный max_user_id, с которым стучится клиент, чтобы вывести его на экран пользователя для последующего ручного занесения в БД.

[STRICT RULES OF ENGAGEMENT]

NO REFACTORING: Не изменяй архитектуру, стили написания, импорты или логику проверок, если это не указано явно в задаче.

NO DELETIONS: Не удаляй существующие проверки (особенно if (!maxUserId), if (error || !user), if (!user.is_active)).

NO MOCKING: Не пиши «костыли», которые принудительно возвращают status: 200 или пропускают пользователя. Нам нужен реальный отказ системы, но с более информативным текстом ошибки.

FULL FILE OUTPUT: В ответе предоставь полный, готовый код измененного файла, без сокращений (без комментариев вида // ... остальной код ...), чтобы я мог просто скопировать и вставить его.

[TARGET FILE]
Файл авторизации МАХ (вероятно, apps/miniapp/app/api/auth/max/route.ts).
В нем находится export async function POST(request: Request).

[ACTIONABLE TASKS]
Шаг 1: Добавь логирование на сервере
Сразу после извлечения maxUserId (строка const maxUserId = body.initData?.max_user_id;), добавь логирование:
console.log('[MAX Auth Debug] Incoming maxUserId:', maxUserId);

Шаг 2: Модифицируй текст ошибки 403 (User not found)
Найди блок кода, который срабатывает, если пользователя нет в базе (ошибка базы или !user):

TypeScript
if (error || !user) {
return NextResponse.json(
{ error: 'Доступ запрещён. Обратитесь к администратору.' },
{ status: 403 },
);
}
Измени строку error так, чтобы она динамически выводила пришедший ID.
Новый код должен выглядеть так:

TypeScript
if (error || !user) {
return NextResponse.json(
{ error: `Доступ запрещён. Ваш MAX ID: ${maxUserId}. Сообщите его администратору.` },
{ status: 403 },
);
}
[EXPECTED DELIVERABLE]
Выведи только полный обновленный код файла route.ts. Никаких дополнительных рассуждений не требуется.
