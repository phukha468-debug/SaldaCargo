📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Заказчик передал точный список автопарка для заливки в БД (тестовые статические данные, но реальные названия и водители). 
Также выявлено, что при одобрении рейса (`/api/trips/[id]/approve/route.ts`) одометр машины не обновляется (там стоит заглушка).
Кроме того, Архитектору требуется полная карта проекта для избежания ошибок в будущем.
</context>

<task>
1. **Инвентаризация проекта:**
   - Прочитай все файлы в директории `docs/`. В консоли выведи краткий лог: "Файлы docs/ прочитаны, структура понятна" (или укажи на ошибки, если они нечитаемы).
   - Просканируй файловую систему и создай новый файл `docs/PROJECT_STRUCTURE.md`. Запиши туда полное дерево директорий и ключевых файлов проекта (Next.js роуты, компоненты, API, packages), чтобы Архитектор понимал, что уже реализовано.

2. **Заливка Автопарка (Seed):**
   - Внеси в таблицу `assets` реальный список машин. Статичные данные (VIN, страховки) заполни тестовыми заглушками, чтобы проверить UI.
   - Валдаи: 096 (6м, Семён), 188 (6м, Слава), 446 (Самосвал, Вова), 883 (5м, Женя).
   - Газели: 009 (4м, Саша), 098 (4м, Лёха), 051 (4м, Денис).
   - Старые Газели: 866 (3м, Владимир), 877 (3м, Фарух), 914 (3м, Амир).
   - Проектная: Газель 223 (статус 'repair' или 'резерв').

3. **Синхронизация рейса и Дашборда:**
   - Открой `apps/web/app/api/trips/[id]/approve/route.ts`.
   - Добавь логику: после успешного обновления `status = 'approved'`, получи `vehicle_id` и `end_odometer` из этого рейса.
   - Сделай UPDATE в таблице `assets`: установи `odometer_current = end_odometer` для соответствующего грузовика.

4. **Проверка API:**
   - Убедись, что эндпоинт `/api/fleet/stats` корректно отдает данные для `page.tsx` (включая массив с 13 машинами и их метриками).

5. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- Не создавать новые колонки в таблице `assets` (например, VIN), если их там еще нет. Работай строго с текущей `schema.sql`.
- В `route.ts` используй `createAdminClient` для обновления `assets`, чтобы обойти RLS, так как это системная операция после подтверждения админом.
- Исполнитель: Claude Code.
</rules>