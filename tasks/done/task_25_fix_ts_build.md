# 🛠 ЗАДАЧА 25: Исправление TypeScript ошибки билда

## 1. Контекст
**Проблема:** Деплой на Vercel упал с ошибкой `exit code: 1` на строке `router.push(routes[0].path);`. 
**Причина:** Строгий TypeScript ругается на нетипизированный массив `routes`.

## 2. Действие
В файле `apps/miniapp/app/page.tsx` найди инициализацию массива `routes` (внутри `useEffect`, где проверяются роли) и добавь ей явный интерфейс:

```typescript
// БЫЛО:
const routes = [];

// СТАЛО:
const routes: { path: string; label: string }[] = [];
3. Ожидаемый результат
Внеси это крошечное изменение.

Обязательно запусти pnpm run build локально и убедись, что сборка miniapp проходит на 100%.

Сделай коммит и push.