# 🛠 ЗАДАЧА: [Короткое название задачи, например: Отладка MAX ID]

## 1. Контекст и Роль
**Роль:** Senior Fullstack Developer (Next.js, TypeScript, Supabase).
**Проект:** SaldaCargo (MiniApp).
**Проблема/Цель:** [Опишите суть проблемы и цель текущей задачи. Например: Пользователи получают 403 ошибку при входе. Цель — вывести приходящий max_user_id на экран для отладки].

## 2. Строгие правила (CRITICAL RULES)
1. **NO REFACTORING:** Не изменяй архитектуру, стили написания, импорты или логику проверок, если это не указано явно в шагах.
2. **NO DELETIONS:** Не удаляй существующие проверки, условия, переменные или логику, если об этом нет прямого указания.
3. **NO MOCKING:** Не пиши «костыли» (например, принудительный возврат status 200). Решай проблему архитектурно или согласно инструкции.
4. **DIRECT EDITS:** Применяй изменения напрямую к указанным файлам.

## 3. Целевые файлы (Target Files)
Пожалуйста, прочитай и проанализируй следующие файлы перед началом работы:
- `[Относительный путь к файлу 1, например: apps/.../route.ts]`
- `[Относительный путь к файлу 2, если требуется]`

## 4. Пошаговое выполнение (Actionable Steps)

**Шаг 1: [Название действия, например: Добавление логирования]**
- **Локация:** [Где именно менять, например: Сразу после объявления переменной `maxUserId`]
- **Инструкция:** [Что конкретно сделать]
- **Ожидаемый код:**
  ```typescript
  // Вставь сюда пример точного кода, который хочешь получить
Шаг 2: [Название действия, например: Изменение текста ошибки]

Локация: [Где именно менять]

Инструкция: [Что конкретно сделать]

5. Ожидаемый результат (Deliverables)
После выполнения изменений, выведи в чат отчет в следующем формате, чтобы я мог провести ревью:

Какие файлы были прочитаны (ReadFile).

Какие файлы были изменены и diff изменений (WriteFile -> Accepted).

Выведи полный обновленный исходный код измененных файлов.